# Skills loop-engineering refactor

A loop-engineering review of the 17 nix-managed skills, run as a scored loop: six frozen axes with checklist KPIs (review-rubric.md), fresh-context reviewer subagents per axis (maker-checker), plan refactored each round, stop when every axis's plan-projected score is ≥ 80 (hard cap 4 rounds). **Converged at round 2: all six axes ≥ 80.** Round-2 residual findings were folded into the `## Next` item wordings post-gate as strictly-additive sharpenings (see Notes). All DRs resolved — see decisions.md.

## Review scores (gate: every plan-projected axis ≥ 80 — PASSED at R2)

| axis | wave-1 baseline | R1 current | R1 plan (pre-refactor) | R2 current | R2 plan |
|---|---|---|---|---|---|
| loop closure | 72 | 70 | 90 | 90 | **100** |
| termination & triggers | 58 | 50 | **50 ✗** | 50 | **90** |
| state & resumability | 66 | 90 | 80 | 70 | **90** |
| measurability | 60 | 60 | **70 ✗** | 30 | **90** |
| composability | 63 | 80 | 90 | 60 | **90** |
| overhead | 70 | 70 | 80 | 70 | **90** |

Scores are measured by fresh reviewer subagents against review-rubric.md each round — never hand-edited. Current-state columns vary between rounds (reviewer strictness noise on "partial vs fail across the set"); both rounds rank the same gaps, and the gate rides on the plan column. The R1 plan column scored the old W4–W11; the R2 columns scored the refactored W7–W16 as committed in 9b61944.

## Done

- [x] W0 — mechanism inventory verified: no `Workflow` tool, no `/goal`; real engines are Agent fan-out, ralph-loop, /loop+ScheduleWakeup, CronCreate, Routines, Tasks — verified in this environment via ToolSearch; re-confirm the subagent-nesting fast path interactively before relying on it — SUPERSEDED by W17 on the Workflow//goal claims: both are real as of Claude Code 2.1.201; the ToolSearch-only method cannot see CLI built-ins
- [x] W1+W6 — issue-resolver and issue-loop rebuilt on Agent fan-out (prompt+parse contract, MAX_PARALLEL=10, main-session orchestration, mandatory plan.md state, explicit outer stop predicate); dead Workflow JS deleted — 8e2c579
- [x] W2 — replay-prompt dangling references fixed — 913c2fc
- [x] W3 — loop-automation engine table updated (ralph-loop, /loop; stale wording refreshed) — 388260b
- [x] R1 — round-1 six-axis review (6 fresh reviewer subagents, facts-only inventory as shared ground truth); 2 axes below gate; plan refactored: W4/W8/W10/W11 sharpened, W12–W16 added — 9b61944
- [x] R2 — round-2 six-axis review of the refactored plan; ALL SIX ≥ 80 (100/90/90/90/90/90) → loop stop condition fired; 10 residual findings folded into item wordings post-gate — a82b9da
- [x] W17 — mechanism truth update (DR Option A, 2026-07-07): false "no Workflow, no /goal" negations removed from issue-loop, issue-resolver, loop-automation; /goal + Workflow mapped as verified engines (loop-automation engine rows + Anthropic's turn/goal/time/proactive frame; issue-loop /goal outer-loop option; issue-resolver engine note). Default architecture unchanged: main-session Agent fan-out — this commit

## Next

- [ ] W7 — single-source the commit gates in tidy-first (it owns the never-commit-standalone-Red rule); tdd links to it; commit-message stays format-only. Check: the three gate conditions appear in exactly one file
- [ ] W9 — plan-state: bound retries — item fails → switch to debug (existing path) → fix fails twice → Blocked/DR, move on. Check: the two-failure bound is stated in plan-state
- [ ] W14 — compaction-safe iteration: plan-state Execution gains step 0 "re-read plan.md at the top of every iteration (it may have been compacted or edited concurrently)"; issue-resolver (loop steps), issue-loop (batch step), and safe-lib-upgrade's convergence loop (re-read the addressed-gaps ledger each iteration; lands with W4) reference it. issue-resolver's per-round record becomes a minimal template (round | axis | score | confirmed findings) under `## Notes`. Check: all four files carry the re-read rule; template present
- [ ] W13 — greppable termination surface: every process skill (list in W10) carries labeled `Stop:` / `Bound:` / `Stuck:` lines. tdd's become objective (Stop: test list empty ∧ suite green; Bound: same test red after 3 attempts → Stuck: switch to debug). debug gets an investigation budget (Bound: 5 hypotheses without progress or unreproducible → Stuck: DR + honest report) AND writes ruled-out hypotheses to the task plan.md `## Notes` as `Do NOT:` lines as they are ruled out (not only at the Stuck boundary). Non-until-loop members get specified text, not ceremony: loop-automation's markers point at the hosted loop's predicate; tidy-first's Stop = tidying queue empty ∧ tests green — or an explicit one-line exemption with rationale. In issue-resolver, markers REPLACE existing prose lines (budget constraint, see Notes) and must NOT evict the mid-loop state-record line. Check: every process skill greps all three markers (or a recorded exemption); tdd/debug bounds present; debug dead-end rule present
- [ ] W15 — scored-verdict contract: maker-checker's checker returns one fenced json block (verdict pass/fail, findings with file:line, confidence) with the one-re-ask parse rule (same DR as issue-resolver); add the orchestration-locus line (the orchestrating/main session dispatches the checker; a subagent-maker hands the artifact up rather than nesting). replay-prompt Phase-4 scores are marked advisory-only with a deterministic pick rule (max total; tie → shorter prompt), never aggregated across contexts. Check: json contract + locus line present in maker-checker; "advisory" wording in replay-prompt
- [ ] W16 — replay-prompt loop closure: any Phase-5 revision re-runs the full Quality Checklist; Bound: ≤ 2 revision passes → Stuck: ship best variant with residual "no" items listed. Check: re-check loop with cap and stop present
- [ ] W12 — plugin-sibling disambiguation, at SELECTION time and in body: for tdd, debug, git-wt, plan-state, maker-checker (twins: superpowers:test-driven-development / systematic-debugging / using-git-worktrees / writing-plans+executing-plans; pr-review-toolkit + superpowers:requesting-code-review) add BOTH (a) a short anti-trigger clause in the frontmatter `description` (skill selection reads only frontmatter — e.g. "distinct from superpowers:X — prefer X when …") and (b) a one-line `Overlap:` note in the body stating when to prefer each. maker-checker's Overlap also gets a crit line (crit = interactive inline human review of a diff/plan; maker-checker = automated fresh-context pass/fail gate). pr-dependency-review — a functional (no-name-twin) overlap with pr-review-toolkit:review-pr, /code-review, /review, crit via the shared "review this PR" trigger — gets its own Overlap line (dependency/complexity visualization, not bug/quality review) and keep/remove DR. Log one keep/remove DR per pair in decisions.md recording what the repo skill adds (Kent-Beck CLAUDE.md conventions, git-wt CLI, plan.md contract, checker contract); any recommendation to disable a twin is a DR output for the user, not an action. Check: grep finds the frontmatter clause AND `Overlap:` in all 5 + pr-dependency-review; decisions.md has 6 pair DRs
- [ ] W4 — adopt safe-lib-upgrade into the nix-managed skills dir (delete the hand-dropped `~/.claude/skills/safe-lib-upgrade/` first: real files collide with home-manager's recursive linking), sharpened per R1+R2: add plan-state integration (persist breaking-change table, audit table, and addressed-gaps ledger to the repo-root plan.md so the human merge-gate pause and compaction are resumable; Integration section referencing plan-state), W13 termination markers (Stop: gaps = 0; Bound: a fix failing twice or convergence stalling → Stuck: Blocked/DR + honest report), an `Overlap:` line vs maker-checker ("safe to merge" collision), and an orchestration-locus line (phases are dispatched by the orchestrating/main session; nesting is a verify-first fast path — same wording family as issue-resolver). Check: nix link works, old dir gone, skill greps Stop/Bound/Stuck + plan-state + Overlap + locus
- [ ] W8 — japanese-technical-writing: append a revise loop with separation — a fresh-context checker subagent receives ONLY the manuscript + the section checklist (never the author's rationale) and returns violations with locations; write → check → fix → re-check; Stop: a pass with zero new violations; Bound: 3 passes → Stuck: DR + residual violations listed. Check: loop present with markers and maker-checker separation
- [ ] W10 — evals + ledgers, runnable not just present: process-skill denominator is enumerated here = {issue-loop, issue-resolver, maker-checker, plan-state, debug, loop-automation, replay-prompt, tdd, tidy-first, japanese-technical-writing, safe-lib-upgrade}; ≤ 2 justified exclusions allowed, recorded in decisions.md (extract-skill-from-session is excluded now: proposal-only, no loop, its 6-criteria bar is its own check). Two scenario types with pinned schemas: (a) trigger/selection evals run by skill-creator's `run_eval.py` — schema `query` / `should_trigger`, near-miss = a should-NOT-fire scenario; (b) behavioral evals via the benchmark flow with graded `expectations`. Convert issue-resolver's existing evals.json to the pinned schema (today it runs under neither mechanism). A "valid eval" = executes to a pass/fail under its named runner (not merely parses). Each covered skill gets `evals/` with both files as applicable plus `evals/ledger.md` recording actual run results. Required behavioral scenarios: plan-state "resume a half-done plan.md in fresh context without re-planning"; loop-automation near-miss "loop kept state in conversation memory and lost it on cold start". KPI: coverage = skills-with-valid-evals / denominator ≥ 80%, machine-computable. Check: coverage KPI computes green from the files alone; every evals.json schema-validates against its runner
- [ ] W11 — skill-lint script + read-only GitHub Action (DR approved). Deterministic checks: (1) frontmatter name = dir; (2) skill-name refs resolve against a registry file checked into the repo (repo skills + managed extras + plugins + built-ins + subagent types; whitelists the empirical-prompt-tuning pin); (3) tool/mechanism refs against an allow-list config derived from the W17-re-verified mechanism inventory (includes the Workflow tool and /goal built-in; verification per class — tools via ToolSearch, CLI built-ins via docs); (4) line budget warn > 270, fail > 300; (5) for process-skill-list members: evals.json schema-validates against its named runner's required keys, contains ≥ 1 near-miss, and ledger.md records a real run (runnable, not just parseable); (6) termination surface: Stop/Bound/Stuck markers present or a recorded exemption (W13); (7) overlap warn: name collisions against the plugin sibling map (W12) AND shared trigger phrases across skills+plugins+built-ins (exact-phrase match, warn-only). Trigger-phrase *quality* stays warn-only (semantic). Action: PRs + weekly off-peak cron + workflow_dispatch kill switch, `permissions: contents: read`. KPIs emitted: dangling refs = 0, eval coverage %, budget violations, marker coverage. Check: lint green locally on the finished tree; Action lands read-only

## Blocked / DR

- (none — 7 DRs resolved, logged in decisions.md)

## Notes

- MAX_PARALLEL = 10 is a user decision (DR); do NOT "correct" it back to 4.
- Workflow tool + /goal built-in are VERIFIED REAL (Claude Code 2.1.201; official loops article + docs + this session — DR Option A, 2026-07-07). Skills reference them as engines; the DEFAULT architecture of issue-loop/issue-resolver stays main-session Agent fan-out, and no skill may REQUIRE Workflow (explicit user opt-in only). Verify mechanisms per class: tools via ToolSearch, CLI built-ins via docs or typing the command.
- Do NOT: touch golang-pr-review (user instruction — excluded from scope: no adopt, no refactor, no evals).
- Do NOT: change the review axes or rubric mid-loop (review-rubric.md is frozen; a moving yardstick lets the loop game itself to 80).
- Deferred deliberately (not unfinished work): pr-body self-check; safety-rails ×3 dedup (cross-referenced on purpose); .agents/skills codex-portability rewrite.
- empirical-prompt-tuning stays third-party-pinned (home.nix:127); skill-lint must whitelist it.
- Budget constraint: issue-resolver (281) and pr-dependency-review (284) sit near the 300-line cap — new content for them goes to references/, and W13 markers replace prose lines rather than adding.
- Scope rulings for scoring (R1, aggregator-confirmed; R2 reviewers applied them): installed plugin siblings are in-scope for trigger/overlap checks; tdd and debug count as loop skills; W4 pulls safe-lib-upgrade into the scored set (some R2 reviewers also counted it in current since it is on disk — noted, harmless).
- Post-gate folds (R2): the 10 residual R2 findings were folded into W4/W10/W11/W12/W13/W14 wordings AFTER the gate passed — strictly additive sharpenings of the exact checks that withheld points; not re-scored because the stop rule fired. An optional confirming round can re-run the six reviewer prompts (they are reproducible from review-rubric.md + this file) before or after implementation.
- Review-loop artifacts: review-rubric.md (frozen axes + KPIs), decisions.md (DR log). Reviewer scores land only in the table above, from subagent JSON.
- Implementation order = the `## Next` order above (W7 → W11); W11 lands last because its lint config consumes W10's denominator, W13's markers, and W12's sibling map.
